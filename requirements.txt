# Python dependencies for the ZTF alert analysis scripts.
# Originally written in 2020; these scripts run fine on current versions.
#
#   pip install -r requirements.txt

numpy
matplotlib
astropy        # FITS I/O and WCS (used by src/fits.py)
scikit-learn   # DBSCAN clustering experiment (used by src/fits.py)
